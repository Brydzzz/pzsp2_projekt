def run_algo_comp(
    graph_fname: str,
    intents_fname: str,
    true_pareto_fname: str,
    iterations: int,
    plot_data: bool,
) -> None:
    print("Running algorithm comparison...")
    print(
        f"gf:{graph_fname}, if:{intents_fname}, tpf: {true_pareto_fname}, it:{iterations}, plot:{plot_data}"
    )
    pass
